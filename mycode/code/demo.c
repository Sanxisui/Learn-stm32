#include <stdio.h>

void main()
{
    // // FILE *fp1 = fopen("demo.txt", "w");
    // // FILE *fp2 = fopen("demo.txt", "w");

    // // fprintf(fp1, "%s", "hello");
    // // int a = fgetc(fp2);
    // // if (a > 0) {

    // //     printf("%c", a);
    // // }
    // printf("%d\n", sizeof(FILE));

    // // fclose(fp1);
    // // fclose(fp2);
}